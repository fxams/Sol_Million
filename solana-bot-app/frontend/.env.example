NEXT_PUBLIC_CLUSTER=mainnet-beta
# If unset, frontend falls back to clusterApiUrl().
NEXT_PUBLIC_RPC_URL=

# Backend origin (Express). Used by fetch() calls.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8787

